# Handlers for diag_worker role
---
- name: Reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
  become: true

- name: Restart diag-worker
  ansible.builtin.systemd:
    name: diag-worker.service
    state: restarted
  become: true
