[project]
name = "diag-master"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Max SCHMELLER", email = "max.schmeller@tier4.jp" }
]
requires-python = ">=3.10"
dependencies = [
    "ros2-transport",
    "sync-graph",
]

[project.scripts]
diag-master = "diag_master:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv.sources]
ros2-transport = { workspace = true }
sync-graph = { workspace = true }
